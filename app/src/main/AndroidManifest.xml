<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nacos">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".ui.AcercadeActivity"/>
        <activity
            android:name=".ui.DabigatranActivity"
            android:label="Dabigatrán"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.EdoxabanActivity"
            android:label="Edoxaban"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.RivaroxabanActivity"
            android:label="Rivaroxaban"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.ApixabanActivity"
            android:label="Apixaban"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <!-- Quita el error " android studio app is not indexable by google search " -->
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>